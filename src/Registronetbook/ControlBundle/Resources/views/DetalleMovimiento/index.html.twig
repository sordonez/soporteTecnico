{% extends '::base.html.twig' %}

{% block body -%}
    <h1>DetalleMovimiento list</h1>

    <table class="records_list">
        <thead>
            <tr>
                <th>Fecha</th>
                <th>Descripcion</th>
                <th>Anulado</th>
                <th>Fechaanulado</th>
                <th>Id</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for entity in entities %}
            <tr>
                <td><a href="{{ path('admin_detalleMovimiento_show', { 'id': entity.id }) }}">{{ entity.fecha }}</a></td>
                <td>{{ entity.descripcion }}</td>
                <td>{{ entity.anulado }}</td>
                <td>{% if entity.fechaAnulado %}{{ entity.fechaAnulado|date('Y-m-d H:i:s') }}{% endif %}</td>
                <td>{{ entity.id }}</td>
                <td>
                <ul>
                    <li>
                        <a href="{{ path('admin_detalleMovimiento_show', { 'id': entity.id }) }}">show</a>
                    </li>
                    <li>
                        <a href="{{ path('admin_detalleMovimiento_edit', { 'id': entity.id }) }}">edit</a>
                    </li>
                </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

        <ul>
        <li>
            <a href="{{ path('admin_detalleMovimiento_new') }}">
                Create a new entry
            </a>
        </li>
    </ul>
    {% endblock %}
