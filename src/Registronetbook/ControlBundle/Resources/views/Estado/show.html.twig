{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Estado</h1>

    <table class="record_properties">
        <tbody>
            <tr>
                <th>Fecha</th>
                <td>{{ entity.fecha|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ entity.descripcion }}</td>
            </tr>
            <tr>
                <th>Anulado</th>
                <td>{{ entity.anulado }}</td>
            </tr>
            <tr>
                <th>Fechaanulado</th>
                <td>{{ entity.fechaAnulado|date('Y-m-d H:i:s') }}</td>
            </tr>
            <tr>
                <th>Id</th>
                <td>{{ entity.id }}</td>
            </tr>
        </tbody>
    </table>

        <ul class="record_actions">
    <li>
        <a href="{{ path('admin_estado') }}">
            Back to the list
        </a>
    </li>
    <li>
        <a href="{{ path('admin_estado_edit', { 'id': entity.id }) }}">
            Edit
        </a>
    </li>
    <li>{{ form(delete_form) }}</li>
</ul>
{% endblock %}
